import React from 'react'
import { AppCards, AppTitle } from './containers'

export const AppContainer: React.FC = () => {
    return (
        <>
            <AppTitle />
            <AppCards />
        </>
    )
}
